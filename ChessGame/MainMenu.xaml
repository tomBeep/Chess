<Page x:Class="ChessGame.MainMenu"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="350" d:DesignWidth="500"
    Title="MainMenu">

    <Grid Margin="10,0,10,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Border Grid.Column="0" Grid.Row="0" Height="auto" Padding="0" Background="#964B00">
            <Button Click="New_Game" Margin="0,10,0,0" Width="125" Height="25" HorizontalAlignment="Center">Play Chess</Button>
        </Border>
        <Border Grid.Column="0" Grid.Row="1" Height="auto" Padding="0" Background="#CDCDCD">
            <Button Margin="0,10,0,0" Width="125" Height="25" HorizontalAlignment="Center">Info</Button>
        </Border>
        <Border Grid.Column="0" Grid.Row="2" Height="auto" Padding="0" Background="#964B00">
            <Button Click="Exit" Margin="0,10,0,0" Width="125" Height="25" HorizontalAlignment="Center">Exit</Button>
        </Border>
    </Grid>
</Page>
